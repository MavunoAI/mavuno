final class ModelMetadata {
  const ModelMetadata({
    required this.name,
    required this.author,
    required this.version,
  });

  final String name;
  final String author;
  final String version;
}
