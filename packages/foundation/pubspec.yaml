name: foundation
description: Core immutable primitives for the Mavuno Platform.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.8.0 <4.0.0"

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3
